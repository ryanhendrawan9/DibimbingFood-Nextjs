/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "api-bootcamp.do.dibimbing.id"],
  },
};

export default nextConfig;
